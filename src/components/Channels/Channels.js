import React from 'react';
import PropTypes from 'prop-types';
import { Container, Header } from './Channels.css';

const Channels = () => {
  return (
    <Container>
      <Header>Channels</Header>
    </Container>
  );
};

Channels.propTypes = {};

export default Channels;
