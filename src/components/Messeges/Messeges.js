import React from 'react';
import PropTypes from 'prop-types';
import { Container, Header } from './Messeges.css';

function Messeges() {
  return (
    <Container>
      <Header>GLOBAL</Header>
    </Container>
  );
}

Messeges.propTypes = {};

export default Messeges;
